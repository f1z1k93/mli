func int fibonacci(int n)
{
	if (n == 0 or n == 1)
		return 1;
	return fibonacci(n - 1) + fibonacci(n - 2);
}

main()
{
	int i = 0;
	for (; i < 20; i = i + 1)
		print(fibonacci(i), " ");
	print("\n");
	return 0;
}
